/** Bar types created on the Announcement bars page (excludes popup_design, additional_ui, etc.). */
export const STOREFRONT_ANNOUNCEMENT_BAR_TYPES = ["sticky", "marquee", "rotating"];

export const FREE_PLAN_ANNOUNCEMENT_BAR_LIMIT = 2;

/** Must match the key in `shopify.server.js` `billing` config and `billing.request({ plan })`. */
export const PAID_MONTHLY_PLAN_KEY = "paid_monthly";
