query CartInput {
  cart {
    sequentialUnlockLevel: attribute(key: "sce_sequential_unlock") {
      value
    }
    cost {
      subtotalAmount {
        amount
      }
    }
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "default", key: "function-configuration") {
      jsonValue
    }
  }
}
